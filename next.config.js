/** @type {import('next').NextConfig} */
const nextConfig = {
  // 이 부분들을 주석처리
  // output: 'export',
  // distDir: 'docs',
  // trailingSlash: true,
  
  images: {
    unoptimized: true
  }
}

module.exports = nextConfig